<?php
$rates = $block->getExchangeRates();
$datetime = $block->getDateTime();
?>
<div class="FashionStore-exchange-rate">
    <style>
        .FashionStore-exchange-rate {
            background: #1a1a1a;
            color: #fff;
            padding: 60px;
            margin: 0;
        }
        .exchange-header {
            text-align: center;
            margin-bottom: 40px;
        }
        .exchange-header-sub {
            font-size: 10px;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: #c9a96e;
            margin-bottom: 12px;
        }
        .exchange-header-title {
            font-family: 'Be Vietnam Pro', 'Segoe UI', Arial, sans-serif;
            font-size: 44px;
            font-weight: 900;
            letter-spacing: 5px;
            text-transform: uppercase;
            color: #fff7e8 !important;
            text-shadow: 0 4px 18px rgba(0, 0, 0, 0.5), 0 0 24px rgba(201, 169, 110, 0.28);
        }
        .exchange-header-time {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.72);
            letter-spacing: 2px;
            margin-top: 10px;
        }
        .exchange-table {
            width: 100%;
            border-collapse: collapse;
            max-width: 900px;
            margin: 0 auto;
        }
        .exchange-table thead tr {
            border-bottom: 1px solid #c9a96e;
        }
        .exchange-table th {
            font-size: 9px;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: #c9a96e;
            padding: 12px 16px;
            text-align: left;
            font-weight: 400;
        }
        .exchange-table td {
            padding: 14px 16px;
            font-size: 13px;
            border-bottom: 1px solid #2d2d2d;
            letter-spacing: 1px;
        }
        .exchange-table tbody tr:hover {
            background: rgba(255, 247, 232, 0.08);
        }
        .currency-code {
            font-weight: 600;
            color: #c9a96e;
            font-size: 14px;
        }
        .currency-name {
            color: #aaa;
            font-size: 11px;
        }
        .rate-buy { color: #4CAF50; }
        .rate-sell { color: #f44336; }
        .rate-transfer { color: #fff; }
        .exchange-footer {
            text-align: center;
            margin-top: 24px;
            font-size: 13px;
            color: rgba(255, 255, 255, 0.68);
            letter-spacing: 1.4px;
        }
        @media (max-width: 768px) {
            .FashionStore-exchange-rate { padding: 40px 20px; }
            .exchange-table th,
            .exchange-table td { padding: 10px 8px; font-size: 11px; }
        }
    </style>

    <div class="exchange-header">
        <p class="exchange-header-sub">Cập Nhật Liên Tục</p>
        <h2 class="exchange-header-title">Tỷ Giá Ngoại Tệ</h2>
        <p class="exchange-header-time">Vietcombank · Cập nhật: <?= $datetime ?></p>
    </div>

    <?php if (!empty($rates)): ?>
    <table class="exchange-table">
        <thead>
            <tr>
                <th>Ngoại Tệ</th>
                <th>Mua Tiền Mặt</th>
                <th>Mua Chuyển Khoản</th>
                <th>Bán Ra</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($rates as $rate): ?>
            <tr>
                <td>
                    <div class="currency-code"><?= $rate['code'] ?></div>
                    <div class="currency-name"><?= $rate['name'] ?></div>
                </td>
                <td class="rate-buy"><?= $rate['buy'] ?></td>
                <td class="rate-transfer"><?= $rate['transfer'] ?></td>
                <td class="rate-sell"><?= $rate['sell'] ?></td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
    <p class="exchange-footer">Nguồn: Vietcombank · Đơn vị: VND</p>
    <?php endif; ?>
</div>
